"use client";
export const Loader = () => {
  return () => (
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
      }}
    >
      <div>Загрузка...</div>
    </div>
  );
};
