"use client";
export const Loader = () => {
  const Loader = () => (
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
      }}
    >
      <div>Загрузка...</div>
    </div>
  );
  return Loader;
};
